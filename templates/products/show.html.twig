{% extends 'base.html.twig' %}
{% block body %}
   <div class="container">
      <h1 class="text-center">{{ product.title }}</h1>
      <h3>{{ product.price }} €</h3>
      <p>{{ product.description }}</p>
   </div>
{% endblock %}
